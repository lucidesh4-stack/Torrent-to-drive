# Working Rules (set by user)

1. **User describes what they want.**
2. **You tell them how you will do it — break it into steps. NO CODE YET.**
   - **NEW**: This step must now include the **Zero-Regression Protocol (ZRP)** Impact Analysis (Path trace, Type check, Dependency audit).
3. **User asks follow-up questions or approves the plan.**
4. **You provide before/after comparison** so only the requested modifications are made.
5. **Summarized answers only** — not detailed explanations.
6. **Code in the background, not in the conversation** — update workspace files only.
7. **Workspace contains a `project.zip`** that auto-updates whenever any file changes.
8. **Only perform actions that won't time out the chat.**
9. **When I upload a `.txt` that is actually an archive (Arena.ai workaround), extract it into the workspace first.**
10. **Never assume — ask questions** when requirements are ambiguous or missing info that would affect correctness.
11. **Follow the Zero-Regression Protocol (ai/PROTOCOL.md)** for every single change.
