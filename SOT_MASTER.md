# 🧠 SOT_MASTER: CloudFlow Single Source of Truth

> This is the High-Density Context Map for the inheriting agent. 

## 🏗️ Core Architecture
CloudFlow is a Flask-based SPA designed as a high-performance Seedr client.

### 1. Backend Stack (Python/Flask)
- **Pattern**: Service-Route Decoupling.
- **API Layer**: `routes/` (Blueprints). Purely handles request/response.
- **Logic Layer**: `search_service.py` and `cloud_service.py`. Pure business logic.
- **Persistence**: Upstash Redis (`redis_store.py`). Used for sessions, magnet history, and logging.
- **Security**: `security.py` implements token guards and config validation.

### 2. Frontend Stack (JS/CSS)
- **Pattern**: Fragmented JS $\rightarrow$ Bundled `app.js`.
- **Build Process**: `build_js.py` concatenates `src/*.js` fragments. **CRITICAL**: Do not edit `app.js` directly; edit the fragments in `src/`.
- **UI**: Vanilla JS with a custom accordion-based grouped renderer for Series mode.

### 3. The "Series Mode" Logic (Highest Complexity)
The Series search is a multi-round orchestration:
- **Sequence**: Broad Query $\rightarrow$ Pack Queries $\rightarrow$ Encoder Queries.
- **Grouping**: Encoder $\rightarrow$ Quality (4K/1080p/720p) $\rightarrow$ Season $\rightarrow$ Episode.
- **Dedup**: Highest-seeder wins per `<series>+SxxExx` per encoder.
- **Relevance**: Exact title matching (prefix for movies) to eliminate spin-offs.

## 🚦 Operational Status
- **Stability**: Hardened. Architecture refactor complete.
- **Active Guard**: The Zero-Regression Protocol (ZRP) is the only way to maintain this stability.

## 📋 Decision Ledger (Key Trade-offs)
- **Provider Failover**: We moved from "Merge all" to "First-provider-to-yield-results" to kill cross-source duplicates.
- **Normal Mode**: Simplified to a single broad query $\rightarrow$ local quality filter $\rightarrow$ size-ascending display.
- **Daily Meter**: Removed for better UX; now using raw provider counts for debugging.
- **Log Access Hardening**: Restricted log download credentials checking to prevent blank login bypasses and timing attacks by validating configured credentials exist before doing `hmac.compare_digest` comparison.
- **Wave 1 Cleanup**: Purged unused functions and imports (including legacy `multi_search`, rate limiter `prune`, `stable_json_dumps`, and redundant local imports) to reduce technical debt and codebase noise.
- **Wave 2 Performance**: Added `@lru_cache` to `parse_release` (avoiding redundant regex calculations during sorting/grouping) and optimized `list_items` to pull storage quota data directly from `list_contents` (saving a redundant `get_settings` API call to Seedr).
- **Desktop Search UI Redesign**: Transitioned the search view from a sparse table layout to a dense dashboard app layout. Replaced the search dropdowns with a collapsible left sidebar filters panel and centered Quality navigation chips above results. Reshaped result rows as high-density cards containing an explicit Encoder column. Integrated DocumentFragment rendering to ensure smooth batch UI insertions. Modified `responsive.css` to hide the sidebar and Encoder column under 768px while maintaining the mobile filter sheet and responsive season chips.
- **UI/UX Upgrades (Batches 1-3)**: Refined typography using Google Font **Outfit**, established standard radii `--radius-*` variables, boosted muted text contrast, and refined accent colors. Aligned desktop filters and modes under a unified `.controls-group` container (with mobile `display: contents` grid preservation). Standardized spacing, resolved header-to-row alignments, and removed lopsided layout padding. Designed custom scrollbars, added smooth fade-in results animations, updated overlays with glassmorphic blurs, implemented a shimmering skeleton mockup search placeholder, and refactored the Add-to-Seedr actions to utilize platform-independent vector SVGs inside fixed-dimension buttons to eliminate layout-jitter.
- **Mobile UI/UX Elevation & Fixes**: Redesigned local Magnet History modal to use uniform SVG action buttons (Copy, Plus, Trash) and styled modal Close actions with a ghost SVG close layout. Configured mobile filter sheets to utilize spring-based slide entrance and exit CSS animations with glassmorphic backdrop blurs. Transformed checkable input controls in mobile filters into custom toggle chips using `:has(input:checked)`. Shielded loading skeleton rows from seeder triangle pseudo-element leaks, added active sweep shimmer gradient animations, and aligned suggest container widths.
- **The Great Unification (UI/UX Finish)**: Executed visual unification across all viewports. Replaced native platform emojis with uniform inline vector SVGs for cloud files list folders and files, toolbar buttons, context items, checked ticks, and bulk menus. Resolved z-index rendering discrepancies that allowed sticky navigation bars to hover over overlay layers. Rectified mobile video modal layout title clipping and wrapping behavior by eliminating inline styles, and converted the close button into a ghost X SVG control. Simplified mobile toolbars by styling secondary buttons uniformly.
- **Tactile Response Overhaul**: Optimized perceived performance to achieve native-like touch response. Applied global `:active` press styles (`transform: scale(0.98)`, `filter: brightness(0.9)`) and standardized all transitions to `all 0.1s ease-out` for all buttons, inputs, filter chips, and rows. Reset tap highlights with `-webkit-tap-highlight-color: transparent`. Wired document-level touchstart listeners to enable instant touch updates on iOS/mobile Webkit browsers. Deferred heavy results list DOM drawing inside quality tabs click hooks to a `setTimeout(..., 0)` tick, and overhauled the "Add to Seedr" button action to update state immediately to a spinner loader optimistically, boosting fluid responsiveness.
- **Jules AI Integration**: Created `.jules/tasks.md` specifying autonomous build pipelines (`build_js.py`), check suites (`check.py`), and deployment/rollback instructions. This sets up context boundaries and automated check expectations for Google Jules AI during PR runs and Render deploy-failure repairs.
- **Polite Detection System**: Transitioned the aggressive clipboard magnet auto-paste feature into a "Polite Detection" system. Removed the intrusive `pointerdown` and `visibilitychange` event listeners in `6-main.js` so that clipboard checks only run on Search tab activation, window refocus, and initial app load. In `5-search.js`, modified `maybeAutoAddMagnet` to prevent automatic search triggering for clipboard and URL sources. Instead, it populates the search query field, sets the UI to show the `+` button, and alerts the user via a toast message (`Magnet detected! Tap [+] to add to Seedr.`) to explicitly confirm the addition.
- **Seedr to Telegram Transfer (MTProto)**: Implemented a memory-efficient, no-disk streaming transfer feature from Seedr directly to Telegram utilizing Telethon (MTProto). Added helper endpoints in `routes/telegram.py` to check authentication status, send verification codes, and verify OTP codes. Telegram sessions are serialized via `StringSession` and stored persistently in Upstash Redis to survive Render restarts. Streaming uploads run in throttled background threads with Redis-based progress tracking. Frontend controls are wired up in `index.html`, `2-cloud.js`, and `6-main.js` to show contextual actions and prompt for phone/OTP login when needed.
- **Telegram Export Hardening (Async & Redis)**: Rebuilt the "Send to Telegram" streaming pipeline. Replaced the synchronous `requests` stream with a non-blocking `AsyncStreamWrapper` leveraging `httpx.AsyncClient` stream iteration (`aiter_bytes`) using a high-performance 1 MB chunk buffer. Upgraded status tracking to a fully persistent Redis-backed schema (`streamly:transfer_status:{task_id}` and `streamly:active_transfer:{sid}` mapping), enabling progress updates to survive frontend page refreshes via automatic polling of `GET /api/transfer/status`. Integrated Telegram Channel support (handling `-100` numeric channel IDs) and pre-upload posting permission validations (`client.get_permissions`) to fail early on unauthorized targets.
- **Double-Buffered Async Transfer Pipeline**: Decoupled the downloading from Seedr and uploading to Telegram by implementing a producer-consumer model in `telegram.py` using `asyncio.Queue` (capped at 5MB). The producer task downloads chunks from Seedr as fast as the connection permits and queues them, while the consumer (`AsyncQueueStreamWrapper.read()`) yields control to the event loop, ensuring the upload network pipe never stalls waiting for disk/stream I/O.
- **RedisStore set API TTL Extension**: Enhanced the `RedisStore.set` method signature to accept an optional `ex: int | None = None` argument. This resolves a `TypeError` in the Telegram active transfer registration and allows keys to be set with a TTL while maintaining backward compatibility for all other 2-argument `.set()` calls across the app.



- **Heuristic-Based Encoder Extraction**: Upgraded the encoder extraction logic in `search_service.py` (`_extract_encoder` and `_normalize_encoder`) from simple patterns to a tiered heuristic approach: Tier 1 checks for brackets/parentheses near the end of the filename (e.g. `[PSA]`, `(ELiTE)`); Tier 2 checks standalone tokens against a comprehensive internal dictionary of known groups (`PSA`, `ELITE`, `MEGUSTA`, etc.) with a dash group fallback; Tier 3 falls back to the rightmost non-metadata/non-digit token relative to the last season/episode marker. To prevent search results with unrecognized encoders from dropping out of the Series mode primary list entirely or falling into "Other / Unparsed", `parse_release` defaults missing/unrecognized groups to `"Other Encoders"` (normalized to `"OTHERENCODERS"`) and flags them as parsed when a season/episode/pack is present.
- **Country Suffix Tolerance in Title Matching**: Resolved an issue where titles with country-specific suffixes (e.g., "Euphoria US", "The Office UK") failed exact episode matches when users searched for base titles (e.g. "euphoria"). Modified `_clean_query_tokens` in `search_service.py` and `_tokens` in `routes/search.py` to pop trailing country codes (`us`, `uk`, `ca`, `au`, `nz`) if the token list length exceeds 1, allowing base queries to map directly to localized releases in primary results.
- **Sequential Transfer Queue**: Configured a global Redis-backed queue list (`streamly:transfer_queue`) and a global active uploader lock (`streamly:active_transfer_global`) to run file transfers sequentially (one-at-a-time) at maximum speed, avoiding CPU and network contention while preserving user session status tracking.
- **Parallel Chunk Telegram Uploader**: Implemented parallel file part uploads to Telegram using Telethon `SaveBigFilePartRequest` and a Semaphore of 3 concurrency to bypass round-trip latency and fully utilize the Seedr download bandwidth up to the safe limit.
- **Monthly Bandwidth Thresholds**: Implemented monthly bandwidth checks on the backend (tracked via Redis key `streamly:monthly_bandwidth:YYYY-MM`). If monthly consumption reaches or exceeds 90 GB, users are allowed to export files but receive a warning notification in the UI (`90% of monthly bandwidth (90 GB) has been consumed.`). If consumption reaches or exceeds 99 GB, all subsequent exports are blocked with a `400 Bad Request` and a blocking error notification (`100% of monthly bandwidth (99 GB) has been consumed. Transfer blocked.`).
- **Telegram Transfers Overlay & Speedometer Dashboard**: Implemented a dedicated "Transfers" SPA navigation tab and modal overlay displaying real-time monthly bandwidth usage, active export status (including dynamic upload speed in MB/s calculated between Redis writes), progress bars, and the list of queued transfers. Users can cancel active uploads (halted gracefully via Redis cancellation flags inside Telethon progress callbacks) or LREM queued items. The export target resolves exclusively from the environment config `TELEGRAM_CHAT_ID`.
- **2 GB Export Size Cap**: Implemented a hard 2 GB size check on both frontend (`static/js/src/2-cloud.js`) and backend (`routes/telegram.py`) to block direct exports of files >= 2 GB, protecting bandwidth limits and ensuring MTProto compliance.
- **Premium Design System, Legacy UI Purge & Global Components**: Implemented a unified Design System with specific geometry token scales (`--radius-*` and `--transition-*` tokens). Defined three main **Global Components** in the stylesheet: (1) **The Container Component** (`.panel`, `.player-box`, `.login-card`, `.ui-container`) styled with `rgba(22, 27, 34, 0.85)` background, `24px` backdrop-blur, thin borders, and 16px radius, (2) **The Button Component** (`button`, `.btn`) with uniform 12px radius, `10px 16px` padding, and elastic active-state scaling, and (3) **The Typography Component** (`h1`/`h2`/`h3` hierarchy, `.label-title`, and `.text-meta` metadata rules). Replaced the top bar navigation tabs with a bottom floating glassmorphism pill navigation bar containing custom filled-vs-outline SVG icon states and smooth translateX highlight pill transitions. Overhauled the centered History and Telegram Transfers overlays on mobile viewports into action sheets sliding up from the bottom with spring physics and grabber indicators. Fixed mobile filter chip ghost-hue by bypassing transitions on unselected backgrounds. Removed redundant encoder headings in mobile series grouped views, integrated Up/Refresh buttons directly into the Cloud Drive content header on desktop, styled mobile navigation as a clean header card, offset selection bulk bars above the bottom navigation bar to avoid conflicts, and completely purged legacy UI elements including the duplicate mobile bottom storage toolbar. Spaced all primary views to a strict 8px grid alignment, ensured elegant text truncation on long files on mobile, styled alert banners for success/error statuses, and implemented loading skeleton additions that match the 12px button radius.
- **Telegram Bottom Nav & Visual Harmony Overhaul**: Refined the global design system by overhauling the bottom navigation iconography to use unified, stroke-weight matched (2px) outline-only Lucide SVGs (removing solid blobs). Standardized active tab indicators with a premium translucent diffused light highlight, borderless layout, soft outer glow, and a `inset 0 1px 1px rgba(255, 255, 255, 0.1)` 3D glass reflection. Heightened the glassmorphism backdrop-blur to `30px` and applied a subtle linear gradient to the bar background (`linear-gradient(180deg, rgba(28, 33, 44, 0.75) 0%, rgba(18, 22, 28, 0.75) 100%)`). Centered navigation button contents vertically by floating the bar `bottom: calc(16px + env(safe-area-inset-bottom))` above mobile swipe gestures, removing inner safe-area paddings, and enforcing a tight `4px` label gap. Resolved notification toast overlaps by elevating `.toast` `z-index` to `9999` and positioning it `bottom: calc(100px + env(safe-area-inset-bottom))` to maintain a precise 20px gap above the floating nav bar on all viewports. Floating the mobile search filter button dynamically above the bottom navigation bar (`right: 16px; bottom: calc(96px + env(...))`) with a premium glassmorphic desaturated charcoal button layout, while left-aligning it on desktop next to the collapsible sidebar layout.

